'use strict';

describe('myApp.audio module', function() {

  beforeEach(module('myApp.audio'));

  describe('audio controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});